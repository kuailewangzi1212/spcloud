eureka:
  client:
    serviceUrl:
      defaultZone: http://10.211.55.5:8761/eureka/
  instance:
      prefer-ip-address: true

server:
  port: 8767
spring:
  application:
    name: service-feign-hystrix

management:
  security:
    enabled: false

feign:
  hystrix:
    enabled: true