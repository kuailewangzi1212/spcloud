eureka:
  client:
    serviceUrl:
      defaultZone: http://10.211.55.5:8761/eureka/
  instance:
      prefer-ip-address: true

server:
  port: 8764
spring:
  application:
    name: service-ribbon

management:
  security:
    enabled: false